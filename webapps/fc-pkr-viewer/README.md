# MVD Browser

Federated Catalog / Public Key Registry viewer.

## Changes

- The debug delete actions are shown as single buttons:
  - `Debug: Delete All Federated Catalog`
  - `Debug: Delete All Public Key Registry`
- The buttons remain on the same row as the entries summary.
- Search, Show All, tab switching, pagination, and delete functions are unchanged.
